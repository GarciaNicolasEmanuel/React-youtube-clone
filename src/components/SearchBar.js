import React from 'react';

import { Paper, TextField } from "@material-ui/core";

class SearchBar extends React.Component {
    state = {
        searchTerm: '',
    }

    render(){
        return(
            <h1>This is a search bar comm</h1> 
        )
    }
}

export default SearchBar;